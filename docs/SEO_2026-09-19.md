# SEO action plan — 2026-09-19

Baseline: `docs/gsc-2026-09-13.md` — 0 clicks, 5 impressions in 28 days, average
position 2.4, sitemap never submitted, indexing unconfirmed. The binding
constraint is discovery and query matching, not site health.

## Code changes made on 2026-09-19

Content integrity (confabulation fixes, zero model impact):

- `RESEARCH.md` — OECD case corrected to the Republican Vilnius University
  Hospital (RVUL) pilot; OECD (2024) reference given its real title, *Public
  procurement in Lithuania: Increasing efficiency through centralisation and
  professionalisation*; Bajari et al. (2014) and Fazekas & Blum (2021) now cited
  in the body, not only the reference list.
- `docs/MODEL_PARAMETERS.md` — §3 provision notation matches the decision
  record (`PZP art. 308 ust. 2`); §7.1 states that the zero on the unrestricted
  side is an allocation convention. No parameter, weight or code change.
- `docs/articles/` — Holmström diacritic in article 1; standard `title`
  frontmatter key in the PL practitioner article.

Query-matching metadata:

- Page titles and descriptions in `lib/i18n.ts` rewritten around practitioner
  queries (PL: kalkulator kosztów przetargu, procedura przetargowa PZP,
  koszty postępowania; EN: public procurement procedure cost calculator),
  120–160 character descriptions, model version numbers dropped from titles
  where they were noise. PL/EN pairs stay faithful translations.

Structured data and hreflang:

- JSON-LD: Organization + WebSite in both layouts, ScholarlyArticle on
  `/research`, SoftwareApplication on `/calculator` (`lib/structured-data.ts`).
- `x-default` hreflang added to page metadata and sitemap entries; PL is the
  default, the EN-only `/research` points x-default at itself.
- `/research` serves `og:type: article`; the metadata factory accepts a
  per-page `ogType`.

Content availability:

- All four Shortcasty methodological notes published (`publishedAt:
  2026-09-19`), episode pages now render, planned-topics section hides when
  empty, sitemap lists the four episode URLs with `lastModified`.

## Manual checklist (needs browser access, no repo change)

1. **Submit the sitemap in GSC** (property `sc-domain:procuracost.com` is
   already verified): Sitemaps → `https://www.procuracost.com/sitemap.xml` →
   Submit. This was never done and is the single highest-leverage step.
2. **Request indexing** for `/` and `/calculator` (URL Inspection → Request
   indexing) after the sitemap is submitted.
3. **Link GSC ↔ GA4** (GA4 property `G-TXEHBTT8E7` exists per
   `docs/ANALYTICS.md`; the link is noted there as missing).
4. **Watch the indexing report** for a week; the 2026-09-13 readout said
   "Processing data — check back tomorrow".
5. **Measure Core Web Vitals once** (Lighthouse on the production origin) —
   unknown so far per `docs/AUDIT_2026-09-04.md`.
6. Re-read GSC after 2–4 weeks and record a fresh `docs/gsc-YYYY-MM-DD.md`
   following the established format.

## Next candidates (not done)

- BreadcrumbList JSON-LD on subpages.
- RSS feed for Shortcasty episodes.
- Per-page Open Graph images for `/research` and episodes.
- Resubmitting the two externally unverifiable numeric details (RVUL duration,
  Beuve interval endpoints) against the primary sources before journal
  submission.
